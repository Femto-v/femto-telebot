const { quote } = require("discord.js");

const commands = {
    halo: /^!halo$/,
    help: /^!help$/,
    news: /^!news$/,
    quote: /^!quote$/,
    quake: /^!quake$/,
};

module.exports = commands;
